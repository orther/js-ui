Initial testing of using pure JS with no libs to do animations and interactions for a cross platform mobile UI

# Usage

Use whatever HTTP server to serve up this project as the root directory.

#### Using Python to serve it up

1. From the root of this project start python's simple HTTP server.

    ```
    python -m SimpleHTTPServer
    ```

2. Then point your browser to `http://localhost:8000`
